
name: Mixed inputs

on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Who to greet
        options: 
          - monalisa
          - cschleiden
      message:
        required: true
      use-emoji:
        type: boolean
        description: Include 🎉🤣 emojis
      environment:
        type: environment

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Send greeting
        run: |
         curl -X POST -H 'Content-type: application/json' --data '{"text":"The \"Run Shell Script\" step has started for '${{ github.repository }}' on branch '${{ github.ref_name }}'."}' https://hooks.slack.com/services/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
         echo "${{ github.event.inputs.message }} ${{ fromJSON('["", "🥳"]')[github.event.inputs['use-emoji']] }} ${{ github.event.inputs.name }}"
