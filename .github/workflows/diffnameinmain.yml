name: non default f

on:
  workflow_dispatch:
    inputs:
      Universe:
        type: choice
        required: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Printing universe
        run: |
          echo "${{ toJson(inputs.Universe) }}"
